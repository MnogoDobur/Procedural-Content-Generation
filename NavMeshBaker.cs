using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class NavMeshBaker : MonoBehaviour {
	/*
	public NavMeshSurface[] objects;
	//public TerrainGenerator terainGen;
	// Use this for initialization
	void Start () {
		
	}

	// Update is called once per frame
	void Update () {
		if (TerrainGenerator.addingNew == true) {
			objects = GameObject.FindGameObjectsWithTag ("Chunk");
			for (int i = 0; i <= objects.Length; i++) {
				objects [i].BuildNavMesh ();
				objects [i].gameObject.tag = "ChunkWithNavMesh";
			}
			TerrainGenerator.addingNew = false;
		}
	}
	*/
}
